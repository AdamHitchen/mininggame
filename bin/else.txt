	else if(type==6)
{
	i_bricks item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
}

	else if(type==4)
{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
}

	else if(type==4)
	{
	i_Set Name item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==5)
	{
	i_asd item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==4)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==4)
	{
	i_seelver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==5)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==6)
	{
	i_silver item = new i_name(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==6)
	{
	i_Wood item = new i_Wood(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==6)
	{
	i_Wood item = new i_Wood(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==7)
	{
	i_Sticks item = new i_Sticks(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==6)
	{
	i_Wood item = new i_Wood(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==7)
	{
	i_Sticks item = new i_Sticks(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}
	else if(type==8)
	{
	i_Wooden Pickaxe item = new i_Wooden Pickaxe(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==9)
	{
	i_Workbench item = new i_Workbench(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==10)
	{
	i_StonePickaxe item = new i_StonePickaxe(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

	else if(type==11)
	{
	i_IronPickaxe item = new i_IronPickaxe(game, x1, y1, this);
	items.add(item);
	item.setID(items.size() - 1);
	noItems++;
	}

